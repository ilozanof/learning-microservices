# CustomerService configuration file for Tests:
spring.application.name = customerService

customerService.getInfo.url = /customer/info/
customerService.register.url = /customer/register

# Probability of Failing:
customerService.fail.probability = 50

# Time to sleep in a call to the customer Service:
customerService.sleep.probability = 0
customerService.sleep.milisec = 3000
